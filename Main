<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wael Salih - Photographer Videographer Editor</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <!-- Video Background -->
    <div class="video-background">
        <div style="padding:52.73% 0 0 0;position:relative;">
            <iframe src="https://player.vimeo.com/video/898002243?badge=0&autopause=0&player_id=0&app_id=58479&autoplay=1&muted=1&loop=1&background=1" 
                    frameborder="0" 
                    allow="autoplay; fullscreen; picture-in-picture; clipboard-write; encrypted-media; web-share" 
                    referrerpolicy="strict-origin-when-cross-origin" 
                    style="position:absolute;top:0;left:0;width:100%;height:100%;" 
                    title="sea">
            </iframe>
        </div>
        <div class="video-overlay"></div>
    </div>

    <!-- Navigation -->
    <nav class="main-nav">
        <div class="nav-left">
            <a href="photography.html" class="nav-link">Photography</a>
            <a href="videos.html" class="nav-link">Videos</a>
        </div>
        <div class="nav-center">
            <a href="index.html" class="logo-link">
                <img src="logo_transparent.png" alt="Zam Zam Logo" class="logo">
            </a>
        </div>
        <div class="nav-right"></div>
    </nav>

    <!-- Main Content -->
    <main class="main-content">
        <div class="hero-text">
            <h1 class="name">Wael Salih</h1>
            <p class="tagline">Photographer Videographer Editor</p>
        </div>
    </main>

    <script src="https://player.vimeo.com/api/player.js"></script>
</body>
</html>

/* Page-specific styles */
.page-body {
    background: #000;
}

.page-nav {
    background: rgba(0, 0, 0, 0.9);
    backdrop-filter: blur(10px);
}

.nav-link.active {
    opacity: 1;
    border-bottom: 2px solid #fff;
    padding-bottom: 2px;
}

.page-content {
    min-height: 100vh;
    padding-top: 140px;
    padding-bottom: 4rem;
}

.content-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 3rem;
    animation: fadeInUp 0.8s ease-out;
}

.page-title {
    font-size: 3.5rem;
    font-weight: 400;
    letter-spacing: 0.1em;
    margin-bottom: 3rem;
    text-align: center;
    opacity: 0.95;
}

.gallery {
    display: grid;
    gap: 2rem;
    margin-top: 3rem;
}

.placeholder-text {
    text-align: center;
    font-size: 1.2rem;
    letter-spacing: 0.05em;
    opacity: 0.7;
    padding: 4rem 0;
}

/* Responsive */
@media (max-width: 768px) {
    .content-container {
        padding: 0 2rem;
    }

    .page-title {
        font-size: 2.5rem;
    }

    .page-content {
        padding-top: 160px;
    }
}

@media (max-width: 480px) {
    .page-title {
        font-size: 2rem;
    }

    .placeholder-text {
        font-size: 1rem;
        padding: 3rem 0;
    }
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Photography - Wael Salih</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="page-styles.css">
</head>
<body class="page-body">
    <!-- Navigation -->
    <nav class="main-nav page-nav">
        <div class="nav-left">
            <a href="photography.html" class="nav-link active">Photography</a>
            <a href="videos.html" class="nav-link">Videos</a>
        </div>
        <div class="nav-center">
            <a href="index.html" class="logo-link">
                <img src="logo_transparent.png" alt="Zam Zam Logo" class="logo">
            </a>
        </div>
        <div class="nav-right"></div>
    </nav>

    <!-- Page Content -->
    <main class="page-content">
        <div class="content-container">
            <h1 class="page-title">Photography</h1>
            <div class="gallery">
                <!-- Add your photography content here -->
                <p class="placeholder-text">Your photography portfolio will be displayed here</p>
            </div>
        </div>
    </main>
</body>
</html>

@font-face {
    font-family: 'Ortica';
    src: url('OrticaLinear-Regular.otf') format('opentype');
    font-weight: normal;
    font-style: normal;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Ortica', sans-serif;
    overflow-x: hidden;
    background: #000;
    color: #fff;
}

/* Video Background */
.video-background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
}

.video-background > div {
    width: 100%;
    height: 100%;
}

.video-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.3);
    z-index: 1;
}

/* Navigation */
.main-nav {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    padding: 2rem 3rem;
    z-index: 100;
    background: linear-gradient(to bottom, rgba(0, 0, 0, 0.4) 0%, transparent 100%);
}

.nav-left {
    display: flex;
    gap: 2.5rem;
    justify-self: start;
}

.nav-center {
    justify-self: center;
}

.nav-right {
    justify-self: end;
}

.nav-link {
    color: #fff;
    text-decoration: none;
    font-size: 1rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    transition: all 0.3s ease;
    opacity: 0.9;
}

.nav-link:hover {
    opacity: 1;
    transform: translateY(-2px);
}

.logo-link {
    display: block;
    transition: transform 0.3s ease;
}

.logo-link:hover {
    transform: scale(1.05);
}

.logo {
    height: 60px;
    width: auto;
    filter: drop-shadow(0 2px 8px rgba(0, 0, 0, 0.3));
}

/* Main Content */
.main-content {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10;
}

.hero-text {
    text-align: center;
    animation: fadeInUp 1.2s ease-out;
}

.name {
    font-size: 5rem;
    font-weight: 400;
    letter-spacing: 0.1em;
    margin-bottom: 1rem;
    text-shadow: 0 4px 12px rgba(0, 0, 0, 0.5);
    animation: fadeInUp 1.2s ease-out;
}

.tagline {
    font-size: 1.5rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    opacity: 0.95;
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.5);
    animation: fadeInUp 1.2s ease-out 0.3s backwards;
}

/* Animations */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Responsive Design */
@media (max-width: 768px) {
    .main-nav {
        padding: 1.5rem 2rem;
        grid-template-columns: 1fr;
        gap: 1.5rem;
    }

    .nav-left {
        justify-self: center;
        order: 2;
    }

    .nav-center {
        order: 1;
    }

    .logo {
        height: 50px;
    }

    .name {
        font-size: 3rem;
    }

    .tagline {
        font-size: 1rem;
    }

    .nav-link {
        font-size: 0.9rem;
    }
}

@media (max-width: 480px) {
    .name {
        font-size: 2rem;
    }

    .tagline {
        font-size: 0.85rem;
    }

    .logo {
        height: 40px;
    }

    .nav-left {
        gap: 1.5rem;
    }
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Videos - Wael Salih</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="page-styles.css">
</head>
<body class="page-body">
    <!-- Navigation -->
    <nav class="main-nav page-nav">
        <div class="nav-left">
            <a href="photography.html" class="nav-link">Photography</a>
            <a href="videos.html" class="nav-link active">Videos</a>
        </div>
        <div class="nav-center">
            <a href="index.html" class="logo-link">
                <img src="logo_transparent.png" alt="Zam Zam Logo" class="logo">
            </a>
        </div>
        <div class="nav-right"></div>
    </nav>

    <!-- Page Content -->
    <main class="page-content">
        <div class="content-container">
            <h1 class="page-title">Videos</h1>
            <div class="gallery">
                <!-- Add your video content here -->
                <p class="placeholder-text">Your video portfolio will be displayed here</p>
            </div>
        </div>
    </main>
</body>
</html>
